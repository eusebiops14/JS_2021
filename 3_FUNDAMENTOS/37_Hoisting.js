//Içamento (?)
//quando declaramos uma variavel usando var, ela é içada, de modo que podemos
//primeiro usar a variavel e depois (em linhas posteriores) declarar essa variavel

//var a ;
console.log('a=',a); //nao da erro (no js, outras linguagens, como c daria erro na hora)
var a = 2;
console.log('a=',a);

//quando declaramos com let, a variavel nao sofre icamento.